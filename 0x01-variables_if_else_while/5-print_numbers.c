#include <stdio.h>
/**
 * main - a program that prints all single digit numbers of base 10
 * Return: 0 (sucess)
 */
int main(void)
{
	int a = 0;

	for (a < 10 ; a++)
		printf("%d", a);
	putchar('\n');
	retun(0);
}
